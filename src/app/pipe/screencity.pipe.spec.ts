import { ScreencityPipe } from './screencity.pipe';

describe('ScreencityPipe', () => {
  it('create an instance', () => {
    const pipe = new ScreencityPipe();
    expect(pipe).toBeTruthy();
  });
});
